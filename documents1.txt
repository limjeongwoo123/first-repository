#include <stdio.h>

double Max(double* p, int count)
{
	double  m = -999999999;
	for (int i = 0; i < count; i++)
	{
		if (m < *(p + 1))
		{
			m = *(p + 1);
		}
	}
(4) 파일의 수정 이력 확인 및 비교 방법





}int main(void)
{
	double ary1[5] = { 5.3, 1.4, 7.9, 0.4, -4.5 };
	double ary2[3] = { -4.3, -1.5, -10.7 };
}
int main(void)
{
	double ary1[5] = { 5.3, 1.4, 7.9, 0.4, -4.5 };
	double ary2[3] = { -4.3, -1.5, -10.7 };
}